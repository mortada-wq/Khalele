# Claude Code Instructions

## Git Workflow

- Always commit and push changes directly to the **main** branch (or **master** if main doesn't exist).
- There is only one developer on this project — no need for feature branches or PRs unless explicitly requested.
- Use clear, descriptive commit messages.
- After making changes, always commit and push before finishing.
